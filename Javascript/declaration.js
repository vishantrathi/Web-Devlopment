
let a=10;// let = re assign only + not re declare the same variable
console.log(a);
var b=20;// var = assign + declare both the same variable
console.log(b);
const c=30;//const =not re assign  + not re declare the same variable
console.log(c);